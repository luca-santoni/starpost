# Reference docs 2.2.0 update Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring `docs/StarPost_Documentation.md` current through 2.2.0 — document the Run-batch hover dropdown, the new Express batch window, ZIP/7Z archives (RAR removed), and batch profiles storing report settings — and fix the now-incorrect archive-format text.

**Architecture:** Documentation-only edits to a single Markdown file. Each task is a set of exact find-and-replace edits (plus one inserted subsection), verified with `grep` and anchor checks — there is no code and no pytest.

**Tech Stack:** Markdown (`docs/StarPost_Documentation.md`); GitHub-flavored anchors.

## Global Constraints

- Only `docs/StarPost_Documentation.md` is modified by this plan.
- Brand is written **StarPost** in prose.
- The new heading is exactly `### 3.9b Express batch dialog`; its GitHub anchor is `#39b-express-batch-dialog` (used by cross-references and the TOC).
- Do not touch README.md, §2 Capabilities, `docs/screenplay_plan.md`, or document bug-fix behaviours (empty-space deselect, hover-outline fixes) — all explicitly out of scope per the spec.
- No `git add .` — stage `docs/StarPost_Documentation.md` by name.
- This is prose: match the surrounding tone, bolding style, and link style already used in the file.

## File Structure

- `docs/StarPost_Documentation.md` — the only file. Edits land in: the version stamp (line ~5), the Table of Contents (§3 list, ~line 40), §3.2 Toolbar (~line 254), §3.9a Run batch dialog (~lines 595-651), and a new §3.9b subsection inserted before §3.10 (~line 657).

---

### Task 1: Navigation and version stamp

**Files:**
- Modify: `docs/StarPost_Documentation.md` (version stamp ~line 5; TOC ~line 40; §3.2 Toolbar row ~line 254)

**Interfaces:**
- Consumes: nothing.
- Produces: the toolbar entry and TOC now point at `#39a-run-batch-dialog` (Full Batch) and `#39b-express-batch-dialog` (Express batch) — the latter's target section is created in Task 3.

- [ ] **Step 1: Bump the version stamp**

Replace (near line 5):

```
> Version: **2.1.0**
```
with:
```
> Version: **2.2.0**
```

- [ ] **Step 2: Add the Express batch entry to the Table of Contents**

In the §3 list of the Table of Contents, replace this single line:

```
   - [3.9a Run batch dialog](#39a-run-batch-dialog)
```
with these two lines:
```
   - [3.9a Run batch dialog](#39a-run-batch-dialog)
   - [3.9b Express batch dialog](#39b-express-batch-dialog)
```

- [ ] **Step 3: Rewrite the Toolbar "Run batch" row as a hover dropdown**

In §3.2, replace the entire `| **Run batch** | … |` table row (the line beginning `| **Run batch** |`) with:

```
| **Run batch** | A **hover dropdown** (its menu drops on mouse-over) with two entries: **Full Batch** — opens the [Run batch dialog](#39a-run-batch-dialog), the guided five-tab wizard; and **Express batch** — opens the [Express batch dialog](#39b-express-batch-dialog), a lean window for running a saved batch profile quickly. (To extract straight to the workspace without a wizard, right-click → **Open** a file in the Files panel instead.) The whole button is disabled while a run is in progress. |
```

- [ ] **Step 4: Verify the edits landed**

Run:
```bash
grep -n "Version: \*\*2\.2\.0\*\*" docs/StarPost_Documentation.md
grep -n "3.9b Express batch dialog](#39b-express-batch-dialog)" docs/StarPost_Documentation.md
grep -n "A \*\*hover dropdown\*\*" docs/StarPost_Documentation.md
grep -c "Version: \*\*2\.1\.0\*\*" docs/StarPost_Documentation.md
```
Expected: the first three each print one matching line; the last prints `0` (the old version stamp is gone).

- [ ] **Step 5: Commit**

```bash
git add docs/StarPost_Documentation.md
git commit -m "Docs: version 2.2.0, Run batch dropdown, Express TOC entry"
```

---

### Task 2: §3.9a Run batch dialog corrections

**Files:**
- Modify: `docs/StarPost_Documentation.md` (§3.9a: intro ~line 597; batch-profile bar ~lines 605-609; Summary archive-format bullet ~lines 636-641; Batch run paragraph ~line 646)

**Interfaces:**
- Consumes: nothing.
- Produces: §3.9a now describes ZIP/7Z archives, profiles that store report settings, and the "Full Batch" entry point.

- [ ] **Step 1: Point the intro at "Run batch → Full Batch"**

In the §3.9a intro sentence, replace:

```
Opened from the toolbar **Run batch**. Unlike the [Export dialog](#39-export-dialog)
```
with:
```
Opened from the toolbar **Run batch → Full Batch**. Unlike the [Export dialog](#39-export-dialog)
```

- [ ] **Step 2: Note that batch profiles store the report settings**

Replace the whole "Batch profile bar (top)" paragraph:

```
**Batch profile bar (top).** A **Batch profile** selector with **Load** and
**Save as…** stores a whole Run-batch setup — the chosen reports, and the saved
plots and scenes — under a name for reuse. These batch profiles are **separate**
from the report/plot profiles used by the main view, and are also listed under
Settings → Profiles → **Batch profiles**.
```
with:
```
**Batch profile bar (top).** A **Batch profile** selector with **Load** and
**Save as…** stores a whole Run-batch setup — the chosen reports (along with the
**report format**, **Include units** and **Combined report** settings), and the
saved plots and scenes — under a name for reuse, so loading a profile restores all
of them. These batch profiles are **separate** from the report/plot profiles used
by the main view, and are also listed under Settings → Profiles →
**Batch profiles**.
```

- [ ] **Step 3: Correct the Summary tab's Archive format text**

In the **Summary** bullet, replace this fragment:

```
- **Summary** — a final review before the run. **Export options**: the **Archive
  format** selector (ZIP / 7Z / RAR — **7Z and RAR are not currently supported;
  the run always writes a `.zip`**, with the other formats planned for a future
  release) and **Include dataset .csv** — when ticked, each data set's portable
```
with:
```
- **Summary** — a final review before the run. **Export options**: the **Archive
  format** selector (**ZIP** or **7Z**) — the run writes the chosen format (a
  `.zip` or a `.7z`) — and **Include dataset .csv** — when ticked, each data set's portable
```

(Leave the remainder of the Summary bullet — the StarPost CSV explanation and the read-only Reports/Plots/Scenes lists — unchanged.)

- [ ] **Step 4: Note the chosen format in the Batch run paragraph**

In the paragraph that begins `**Batch run** then extracts/renders`, replace:

```
**Batch run** then extracts/renders as needed and writes a **single archive**
containing **one folder per data set**, each holding its report table, an image
```
with:
```
**Batch run** then extracts/renders as needed and writes a **single archive**
(ZIP or 7Z, as chosen on the Summary tab) containing **one folder per data set**,
each holding its report table, an image
```

- [ ] **Step 5: Verify the corrections landed and the stale strings are gone**

Run:
```bash
grep -n "Run batch → Full Batch" docs/StarPost_Documentation.md
grep -n "report format\*\*, \*\*Include units\*\* and \*\*Combined report" docs/StarPost_Documentation.md
grep -Ec "7Z and RAR|always writes a|planned for a future release" docs/StarPost_Documentation.md
```
Expected: the first two each print one matching line; the third prints `0` (all stale archive-format phrasings removed).

- [ ] **Step 6: Commit**

```bash
git add docs/StarPost_Documentation.md
git commit -m "Docs: correct Run batch archive formats and profile scope"
```

---

### Task 3: New §3.9b Express batch dialog subsection

**Files:**
- Modify: `docs/StarPost_Documentation.md` (insert a new subsection between the end of §3.9a and `### 3.10 Settings dialog`)

**Interfaces:**
- Consumes: the `#39a-run-batch-dialog` anchor (for its cross-link).
- Produces: the `### 3.9b Express batch dialog` heading and its `#39b-express-batch-dialog` anchor, targeted by Task 1's toolbar row and TOC entry.

- [ ] **Step 1: Insert the subsection**

Immediately **before** the `### 3.10 Settings dialog` heading (and after the §3.9a content, including its multi-select blockquote), insert:

```markdown
### 3.9b Express batch dialog

Opened from the toolbar **Run batch → Express batch**. This is the fast path for
users who already have a saved **batch profile**: rather than stepping through the
five-tab wizard, you pick a profile and sources, set the archive options, and run.
The profile supplies everything about the output — the reports (with their
**report format**, **Include units** and **Combined report** settings), the saved
plots, and the saved scenes.

**Layout (top to bottom):**

- **Batch profile** — a selector listing the saved batch profiles (the same ones
  used by the [Run batch dialog](#39a-run-batch-dialog) and Settings → Profiles →
  **Batch profiles**). Choosing a profile is **required**: the **Batch run** button
  stays disabled until one is selected. If no batch profiles exist yet, a note
  directs you to create one in **Full Batch**.
- **Sources** — the same source picker as the wizard's **Source** tab: choose
  **`.sim files`** or **`Loaded data sets`**, load candidates (**Load Files** /
  **Load Data Set**), tick which to include, and use **Select All** / **Clear**.
  There is **no "Has similar format"** step here — the profile already defines the
  outputs, so no file needs to be extracted up front to configure the run.
- **Export options** (in the Options column, beneath the source input) — the
  **Archive format** selector (**ZIP** or **7Z**) and **Include dataset .csv**
  (each data set's portable StarPost CSV, added to its folder in the archive),
  matching the wizard's Summary tab.
- **Batch run** — starts the run.

**Batch run** produces the **same output** as the wizard: a **single archive**
(ZIP or 7Z, as chosen) with **one folder per data set**, each holding its report
table, an image of every saved plot, the saved-scene stills, and (if enabled) the
data set's CSV — plus, when the profile's **Combined report** is on, one all-sims
report at the archive root. As in the wizard, STAR-CCM+ is invoked only when a
`.sim` source must be extracted or a scene rendered (its executable path must be
set in Settings), and the run warns on **No data selected** or when the chosen
profile yields **nothing to output**.

```

- [ ] **Step 2: Verify the subsection and its placement**

Run:
```bash
grep -n "^### 3.9b Express batch dialog" docs/StarPost_Documentation.md
grep -n "^### 3.9a Run batch dialog\|^### 3.9b Express batch dialog\|^### 3.10 Settings dialog" docs/StarPost_Documentation.md
```
Expected: the first prints one line. The second prints the three headings in this order — 3.9a, then 3.9b, then 3.10 (confirming the new section sits between them).

- [ ] **Step 3: Verify the §3.9b cross-references resolve**

The heading `### 3.9b Express batch dialog` is the target for the `#39b-express-batch-dialog` anchor (GitHub lowercases the text, drops the `.`, and turns spaces into hyphens). Confirm both the target heading and the two links to it exist:

```bash
grep -c "^### 3.9b Express batch dialog" docs/StarPost_Documentation.md
grep -c "#39b-express-batch-dialog" docs/StarPost_Documentation.md
```
Expected: the first prints `1` (the target heading), the second prints `2` (the toolbar row and the TOC entry from Task 1, both pointing at it).

- [ ] **Step 4: Commit**

```bash
git add docs/StarPost_Documentation.md
git commit -m "Docs: add Express batch dialog reference section"
```

---

### Task 4: Whole-document verification

**Files:** none (verification only).

- [ ] **Step 1: Confirm no stale strings remain anywhere in the file**

Run:
```bash
grep -Ec "7Z and RAR|always writes a `.zip`|planned for a future release" docs/StarPost_Documentation.md
grep -c "Version: \*\*2\.1\.0\*\*" docs/StarPost_Documentation.md
```
Expected: both print `0`.

- [ ] **Step 2: Confirm the new content and pointers are all present**

Run:
```bash
grep -c "Run batch → Express batch" docs/StarPost_Documentation.md   # expect >= 2 (toolbar + §3.9b)
grep -c "^### 3.9b Express batch dialog" docs/StarPost_Documentation.md  # expect 1
grep -c "3.9b Express batch dialog](#39b-express-batch-dialog)" docs/StarPost_Documentation.md  # expect 1 (TOC)
```
Expected: the counts are `>=2`, `1`, and `1` respectively.

- [ ] **Step 3: Read the edited regions once for prose quality**

Open `docs/StarPost_Documentation.md` and read §3.2, §3.9a, and §3.9b end to end. Confirm the tone/bolding match the surrounding text, the ZIP/7Z description is consistent across all three places, and there are no dangling references to a five-tab wizard for Express batch. Fix any wording nits in place; if a fix is made, re-commit with `git commit -m "Docs: polish 2.2.0 Run batch/Express wording"`.
